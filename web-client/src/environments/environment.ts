export const environment = {
  production: false,
  // Relay server base URL — change to your VPS or Cloudflare Tunnel URL in production
  apiUrl: 'http://localhost:3001',
  wsUrl: 'ws://localhost:3001',
};
